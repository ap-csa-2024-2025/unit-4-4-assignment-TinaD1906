import java.util.Scanner;

public class Main
{
  public static void main(String[] args)
  {
    // You can test your method here
    // System.out.println(isPrime(1));
    // System.out.println(isPrime(17));

    //problem 1
    Scanner scan1 = new Scanner(system.in);
    String sentence1 = scan1;
    int i = 0; 
    int numPvowel = 0;
    while (i<sentence.length()-1)
    {
      if (sentence1.substring(i,i+1).equals("p"))
      {
        if (sentence1.substring(i+1,i+2).equals("a") || sentence1.substring(i+1,i+2).equals("e")|| sentence1.substring(i+1,i+2).equals("i")||sentence1.substring(i+1,i+2).equals("o")||sentence1.substring(i+1,i+2).equals("u"));
        {
          numPvowel = numPvowel + 1;
        }
      }
    }

    //problem 2 
    {
      String given = "Peter Piper picked a pack of pickled peppers.";
      int i = 0;
      int n = 0
      String BadLetters = "etaio"
      while (i<given.length)
      {
        if (given.substring(i,i+1) = Badletters.substring(n, n+1))

      }

    }
  }

  public static boolean isPrime(int N)
  {
    // TODO: Replace and write your code below
    return false;
  }


}
